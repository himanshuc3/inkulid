## Inkulid

A geometry native interpreter language so that you only connect the dots without worrying about the framework.

## Interpreter concepts

1. Tree walking algorithm, where we parse code, extract tokens (acts as nodes), create a syntax. It has the following phases:
   - lexer
   - parwser
   - AST
   - Internal object system
   - Evaluator
